{
  "name": "@analytics-kit/query-api",
  "version": "0.1.0",
  "description": "REST query service — heatmap, zones, sessions, session replay",
  "type": "module",
  "scripts": {
    "dev":       "bun run --watch src/index.ts",
    "start":     "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.4.0"
  }
}
