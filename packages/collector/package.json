{
  "name": "@analytics-kit/collector",
  "version": "0.1.0",
  "description": "Event ingestion service — Hono/Bun, POST /e, queue → storage",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@analytics-kit/storage": "*",
    "hono": "^4.4.0",
    "lz-string": "^1.5.0",
    "ioredis": "^5.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
